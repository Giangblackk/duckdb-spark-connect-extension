# name: test/sql/squawk.test
# description: test squawk extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT squawk('Sam');
----
Catalog Error: Scalar Function with name squawk does not exist!

# Require statement will ensure this test is run with this extension loaded
require squawk

# Confirm the extension works
query I
SELECT squawk('Sam');
----
squawk Sam 🐥

query I
SELECT squawk_grpc_version('Michael') ILIKE 'squawk Michael, my linked gRPC version is %';
----
true
